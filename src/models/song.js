export default class Song {
  constructor(title, artist, pan, bpm) {
    this.title = title;
    this.artist = artist;
    this.pan = pan;
    this.bpm = bpm;
  }

  play() {

  }

  stop(){
    
  }
}
